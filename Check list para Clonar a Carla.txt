Fechado, parça 😎.
Vou montar um **checklist operacional** bem enxuto, com **passo a passo micro**, para você ou qualquer pessoa conseguir **clonar, configurar e rodar** a Carla do zero — localmente e na Railway.

Não terá códigos longos, só instruções claras e ordem correta.

---

# **Checklist Operacional — Projeto Carla**

## **1. Preparar o ambiente**

✅ **Instalar o NVM para Windows**

* Baixar o [NVM for Windows](https://github.com/coreybutler/nvm-windows/releases).
* Durante a instalação, usar o caminho padrão:

  * **NVM root:** `C:\Users\<seu-usuário>\AppData\Local\nvm`
  * **NodeJS path:** `C:\Program Files\nodejs`

✅ **Instalar versões do Node**

```bash
nvm install 18.20.4   # Para Baileys (WhatsApp)
nvm install 20.16.0   # Opcional: para GPT se necessário
```

✅ **Selecionar Node 18** (sempre que for rodar Carla):

```bash
nvm use 18.20.4
```

---

## **2. Clonar o projeto**

✅ **Clonar o repositório do GitHub**

```bash
git clone https://github.com/contatotopofertasloja-web/carla-bot.git
cd carla-bot
```

---

## **3. Configurar variáveis de ambiente**

✅ **Criar o arquivo `.env`** na raiz do projeto e configurar:

```
OPENAI_API_KEY=coloque_sua_chave_aqui
MODEL_NAME=gpt-4o
PRICE_TARGET=170
WPP_AUTH_DIR=./.wpp-auth
MEMORY_TTL_DAYS=7
PORT=8080
```

> **Dica:**
> Para deploy na **Railway**, o `WPP_AUTH_DIR` deve ser:

```
WPP_AUTH_DIR=/app/baileys-auth
```

---

## **4. Instalar dependências**

✅ **Instalar todas as libs necessárias**:

```bash
npm install
```

Dependências inclusas:

* **Express** → Servidor HTTP.
* **Baileys** → Conexão WhatsApp.
* **QRCode** → Geração do QR.
* **OpenAI** → Integração GPT.
* **Dotenv** → Gerenciamento de variáveis.

---

## **5. Conectar ao WhatsApp**

✅ **Criar diretório para salvar sessão**:

```bash
mkdir .wpp-auth
```

✅ **Iniciar o servidor**:

```bash
npm start
```

✅ **Gerar QR**:

1. Acesse: [http://127.0.0.1:8080/wpp/qr](http://127.0.0.1:8080/wpp/qr)
2. Atualize a página a cada 2–3 segundos até o QR aparecer.
3. Escaneie pelo **WhatsApp → Dispositivos Conectados**.

> **Dica:** Uma vez pareado, a sessão fica salva e não precisa repetir.

---

## **6. Testar localmente**

✅ **Verificar status do servidor**:

```bash
curl http://127.0.0.1:8080/health
```

Resposta esperada:

```json
{"ok": true}
```

✅ **Testar envio de mensagem via Webhook**:

```bash
curl -X POST "http://127.0.0.1:8080/webhook" ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\":\"c001\",\"text\":\"Oi, Carla!\",\"context\":{}}"
```

Resposta esperada: pitch de venda personalizado.

---

## **7. Deploy na Railway**

✅ **Passos principais**:

1. Criar projeto **carla-bot** na Railway.
2. Conectar ao GitHub.
3. Configurar **variáveis de ambiente** na Railway:

   * `OPENAI_API_KEY`
   * `MODEL_NAME`
   * `PRICE_TARGET`
   * `WPP_AUTH_DIR=/app/baileys-auth`
   * `MEMORY_TTL_DAYS`
   * `PORT=8080`
4. Criar **Volume persistente**:

   * Path: `/app/baileys-auth`
5. Deploy automático a partir da branch `main`.

✅ **Testar saúde do app na nuvem**:

```bash
curl https://carla-bot-production.up.railway.app/health
```

✅ **Testar Webhook na nuvem**:

```bash
curl -X POST "https://carla-bot-production.up.railway.app/webhook" ^
  -H "Content-Type: application/json" ^
  -d "{\"userId\":\"c001\",\"text\":\"Oi, Carla!\",\"context\":{}}"
```

---

## **8. Recursos de Humanização**

✅ **Simular comportamento humano**:

* Antes de responder, Carla envia presença **“digitando...”** proporcional ao tamanho da resposta.
* Marca mensagens como **lidas** automaticamente.

✅ **Manter memória leve**:

* Histórico do usuário armazenado por **7 dias** para evitar repetição de perguntas.

---

## **9. Persona e Conversão**

✅ **Carla como vendedora real**:

* Sempre fala com empatia e segurança.
* Usa storytelling: “Eu uso esse produto e recomendo!”.

✅ **Produto configurado**:

* Progressiva Vegetal Premium.
* R\$ 170, 500ml, dura 3 meses, sem formol.

✅ **Fluxo de conversão**:

1. Cumprimentar calorosamente.
2. Descobrir necessidades do cliente.
3. Mostrar benefícios do produto.
4. Criar urgência (“estoque limitado”).
5. Encaminhar para link de checkout.

---

## **10. Próximos passos**

* [ ] Criar logs de cliques e conversões.
* [ ] A/B testing com preços diferentes.
* [ ] Configurar múltiplas personas (Carla e Lívia).
* [ ] Criar dashboard com estatísticas em tempo real.

---

Quer que eu já monte esse checklist em um **arquivo separado** chamado `carla-checklist.md` para manter junto do `carla-config.md`?
Assim você terá **o documento técnico** e **o passo a passo prático** lado a lado. Quer?
